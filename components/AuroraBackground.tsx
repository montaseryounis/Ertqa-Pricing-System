// Decorative animated aurora background. Pure decoration; takes no
// pointer events. Disable by removing the import + render in layout.tsx.
export default function AuroraBackground() {
  return <div className="aurora-bg" aria-hidden="true" />;
}
